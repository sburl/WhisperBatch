# Models

WhisperKit downloads Core ML models on first use and caches them locally.
No GGML model needs to be bundled with the application. See ModelManager.swift
for the cache location and supported model management operations.
